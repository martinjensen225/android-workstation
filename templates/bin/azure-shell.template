#!/data/data/com.termux/files/usr/bin/bash

set -Eeuo pipefail

if command -v termux-open-url >/dev/null 2>&1; then
  exec termux-open-url "https://shell.azure.com/bash"
fi

printf '%s\n' "Open https://shell.azure.com/bash in your browser."
